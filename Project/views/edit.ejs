<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md text-sm mb-5 inline-block px-3 py-2" href="/logout">Logout</a>
        </div>
        <h3 class="mb-5">Edit a post</h3>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="what's on your mind?" class="w-1/3 resize-none outline-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%= post.content %></textarea>
            <input class="px-3 w-40 py-2 text-sm bg-yellow-500 text-black block rounded-md mt-2" type="submit" value="Update post">
        </form>
    </div>
</body>
</html>